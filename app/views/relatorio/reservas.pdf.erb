  <meta charset="utf-8">

  <hr>
   <table>
     <td><%=@condominio%></td>
     <td style="font-size: 10px" align="right">Periodo de <%=@data_inicio%> a <%=@data_fim%></td>
   </table>
  <hr>
  <div  class="row">
    <div class="col-lg-12">
         <table class="table table-hover">
           <thead>
             <tr>
               <th align="left">Unidade</th>
               <th align="left">Data reserva</th>
               <th align="left">Local reserva</th>
               <th align="left">Laçamento</th>
               <th align="left">Data cancelamento</th>
               <th align="left">Operador cancelamento</th>
               <th colspan="3"></th>
             </tr>
           </thead>

           <tbody>
               <% @reservas.each do |reserva| %>
               <tr>
                 <td><%= reserva.apartamento.numr_bloco%> - <%= reserva.apartamento.numr_apartamento%></td>
                 <td><%= reserva.data_reserva.blank? ? '' : reserva.data_reserva.strftime("%d/%m/%Y")%></td>
                 <td><%= reserva.tiporeserva.desc_tiporeserva %></td>
                 <td><%= reserva.created_at.blank? ? '' : reserva.created_at.strftime("%d/%m/%Y %H:%M:%S")%></td>
                 <td><%= reserva.data_cancelamento.blank? ? '' : reserva.data_cancelamento.strftime("%d/%m/%Y %H:%M:%S")%></td>
                 <td><%= reserva.user_cancelamento.blank? ? '' : reserva.usercancelamento.pessoa.nome_pessoa%></td>
               </tr>
             <% end %>
           </tbody>
         </table>
   </div>
  </div>


  <style media="screen">

  table {
     display: table;
     border-collapse: separate;
     border-spacing: 2px;
     border-color: gray;
     width:100%;
  }


  .table > tbody > tr > td {
    font-size: 10px;
  }
  .table > thead > tr > th {
    font-size: 10px;
  }

  </style>
